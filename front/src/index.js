import React from 'react';
import ReactDOM from 'react-dom';
 // Caso tenha um arquivo CSS  // O componente App, que você criará

ReactDOM.render(
  <React.StrictMode>
  
  </React.StrictMode>,
  document.getElementById('root')
);
